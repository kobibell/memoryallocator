#include <stdio.h>
#include <stdlib.h>
#include "memory.h"

int main() {
    // Generate 10,000 requests
    for (int i = 0; i < 10000; i++) {
        // Generate allocation/deallocation requests
        // Invoke allocate_mem and deallocate_mem functions
        // Update performance parameters
    }

    // Print statistics
    print_statistics();

    return 0;
}

